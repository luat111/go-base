apiVersion: v1
kind: Secret
metadata:
  name: {{ include "go-base.fullname" . }}-secrets
  labels:
    {{- include "go-base.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
